spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartdelivery
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ${JWT_SECRET:default-dev-secret-key-change-me}
